The following python code on VSCode is giving the error, (AttributeError: 'DataFrame' object has no attribute 'append'), how can I fix it?

tesla_revenue = pd.DataFrame(columns=["Date", "Revenue"])
for row in soup.find("tbody").find_all("tr"):
    col = row.find_all("td")
    date = col[0].text
    revenue = col[1].text
    tesla_revenue = tesla_revenue.append({"Date":date, "Revenue":revenue}, ignore_index=True)

@recommended:languages 
html_data = requests.get(url).text
print(html_data)

soup = BeautifulSoup(html_data, "html5lib")

tesla_revenue = html_data
tesla_revenue

tesla_revenue = Tesla.history(period="max")

tesla_revenue.reset_index(inplace=True)
tesla_revenue.head()

table = soup.find('table')

tesla_revenue = pd.DataFrame(columns=["Date", "Revenue"])
for row in soup.find("tbody").find_all("tr"):
    col = row.find_all("td")
    date = col[0].text
    revenue = col[1].text
    tesla_revenue = tesla_revenue.append({"Date":date, "Revenue":revenue}, ignore_index=True)
    tesla_revenue